{
  "name": "@unison/cli",
  "version": "0.1.0",
  "description": "The `unison` CLI — search and write your Unison brain from the terminal or any coding agent.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "unison": "./src/index.ts"
  },
  "files": ["src", "dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@unison/sdk": "workspace:*",
    "commander": "^12.0.0",
    "open": "^10.0.0",
    "picocolors": "^1.0.0"
  }
}
