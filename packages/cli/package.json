{
  "name": "@unison/cli",
  "version": "0.1.0",
  "description": "The `unison` CLI — search and write your Unison brain from the terminal or any coding agent.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "unison": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --target node --outfile dist/index.js --external commander --external open --external picocolors"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "open": "^10.0.0",
    "picocolors": "^1.0.0"
  },
  "devDependencies": {
    "@unison/sdk": "workspace:*"
  }
}
